{
  "name": "refactor-challenge",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "rimraf dist && tsc",
    "dev": "npm run build && concurrently \"tsc --watch\" \"micro-dev\""
  },
  "dependencies": {
    "@types/micro": "^7.3.3",
    "@types/node": "^14.14.10",
    "concurrently": "^5.3.0",
    "micro": "^9.3.4",
    "micro-dev": "^3.0.0",
    "prettier": "^2.2.1",
    "rimraf": "^3.0.2",
    "typescript": "^4.1.2"
  },
  "prettier": {}
}
